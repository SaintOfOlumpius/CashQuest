<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/transaction_item_margin"
    android:layout_marginVertical="@dimen/spacing_small"
    app:cardCornerRadius="@dimen/transaction_card_corner_radius"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/surface"
    app:strokeWidth="1dp"
    app:strokeColor="@color/primary"
    android:background="@drawable/transaction_item_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/transaction_item_padding"
        android:gravity="center_vertical">

        <!-- Enhanced Category Icon -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="@dimen/transaction_icon_size"
            android:layout_height="@dimen/transaction_icon_size"
            app:cardCornerRadius="12dp"
            app:cardBackgroundColor="@color/primary"
            app:cardElevation="3dp"
            android:background="@drawable/icon_glow_background"
            android:layout_marginEnd="@dimen/spacing_medium">

            <ImageView
                android:id="@+id/categoryIcon"
                android:layout_width="@dimen/transaction_icon_small"
                android:layout_height="@dimen/transaction_icon_small"
                android:layout_gravity="center"
                android:src="@drawable/ic_category_default"
                app:tint="@color/white" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Transaction Details -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginEnd="@dimen/spacing_medium">

            <TextView
                android:id="@+id/descriptionTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/transaction_title_size"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.01"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/categoryTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_small"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/transaction_category_size"
                android:fontFamily="sans-serif-light"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/dateTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_small"
                android:textColor="@color/text_secondary"
                android:textSize="@dimen/transaction_date_size"
                android:fontFamily="sans-serif-light"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <!-- Enhanced Amount Display -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardCornerRadius="8dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/surface"
            app:strokeWidth="1dp"
            app:strokeColor="@color/primary">

            <TextView
                android:id="@+id/tv_cost"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="@dimen/spacing_medium"
                android:paddingVertical="@dimen/spacing_small"
                android:textColor="@color/text_primary"
                android:textSize="@dimen/transaction_amount_size"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.01"
                android:minWidth="80dp"
                android:gravity="center" />
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView> 